# NYC Taxi Fare Prediction API
#
# IMPORTANT: use Python 3.12. Your current venv is Python 3.14, which has no
# prebuilt wheels for scikit-learn 1.6.1 and will try to compile from source.
#
#   py -3.12 -m venv .venv
#   .\.venv\Scripts\Activate.ps1
#   pip install -r requirement.txt

# scikit-learn MUST be 1.6.1 -- the version your .pkl files were trained with.
# Any other version risks silently wrong predictions when unpickling.
scikit-learn==1.6.1

numpy==2.1.3
pandas==2.2.3

Flask==3.1.0
flask-cors==5.0.0

# Production WSGI server (works on Windows, unlike gunicorn)
waitress==3.0.2
